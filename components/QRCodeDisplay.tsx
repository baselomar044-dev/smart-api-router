'use client'import { useEffect, useState } from 'react'import { Smartphone, Copy, Check } from 'lucide-react'export default function QRCodeDisplay() {  const [url, setUrl] = useState('')  const [copied, setCopied] = useState(false)  useEffect(() => {    if (typeof window !== 'undefined') setUrl(window.location.origin)  }, [])  const copyUrl = () => {    navigator.clipboard.writeText(url)    setCopied(true)    setTimeout(() => setCopied(false), 2000)  }  const qrUrl = `https://api.qrserver.com/v1/create-qr-code/?size=180x180&data=${encodeURIComponent(url)}&bgcolor=18181b&color=a855f7`  return (    <div className="flex flex-col items-center gap-4">      {url && <div className="p-3 bg-zinc-900 rounded-xl border border-zinc-700"><img src={qrUrl} alt="QR" className="rounded-lg" width={180} height={180} /></div>}      <div className="flex items-center gap-2">        <code className="px-3 py-1.5 bg-zinc-800 rounded text-sm text-purple-400 truncate max-w-[200px]">{url}</code>        <button onClick={copyUrl} className="p-2 bg-zinc-800 rounded hover:bg-zinc-700">{copied ? <Check size={16} className="text-green-400" /> : <Copy size={16} className="text-zinc-400" />}</button>      </div>      <p className="text-xs text-zinc-500 flex items-center gap-1"><Smartphone size={12} /> Scan to access on mobile</p>    </div>  )}
